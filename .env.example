# ─── Database ──────────────────────────────────────────────────────────────────
# Supabase の場合: Project Settings > Database > Connection string > URI (Transaction pooler)
# 例: postgresql://postgres.xxxx:password@aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true
DATABASE_URL=

# Supabase でマイグレーション用に Direct URL が必要な場合（PgBouncer経由でない接続）
# Project Settings > Database > Connection string > URI (Session pooler) または Direct connection
# 使わない場合は DATABASE_URL と同じ値を入れても OK
# DIRECT_URL=

# ─── Staff login (固定ID/PW) ────────────────────────────────────────────────────
STAFF_ADMIN_USERNAME=admin1
STAFF_ADMIN_PASSWORD=admin123
STAFF_STAFF_USERNAME=staff1
STAFF_STAFF_PASSWORD=staff123

# ─── Anthropic API（任意 — なければ Mock AI モードで動作）──────────────────────
ANTHROPIC_API_KEY=

# ─── App ───────────────────────────────────────────────────────────────────────
# Vercel にデプロイした場合の公開URL（末尾スラッシュなし）
# 例: https://pubtech-inquiry-proto.vercel.app
NEXT_PUBLIC_BASE_URL=

# Session secret（任意 — 現在は base64 のみ使用。将来的に署名付きに移行する場合に使用）
SESSION_SECRET=change-me-in-production-32chars-min!!
